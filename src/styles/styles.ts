import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
  container: {
    flex: 1,
    marginHorizontal: 20,
  },
  header: {
    fontSize: 24,
    color: "#333",
    fontWeight: "bold",
  },
  text: {
    fontSize: 16,
    color: "#333",
  },
  title: {
    fontSize: 20,
    fontWeight: "500",
    color: "#000",
  },
});

export default globalStyles;
